# Analytics is Vercel Web Analytics — no env vars needed.
# Enable it in the Vercel dashboard (Project → Analytics) and ship the
# <Analytics /> component (already in app/layout.tsx). In local dev it is a
# no-op; on Vercel it reports automatically. Custom events live in
# lib/analytics.ts.
