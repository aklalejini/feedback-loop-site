# Required: LLM access for the reviewer + manager.
ANTHROPIC_API_KEY=

# Analytics: the site uses Vercel Web Analytics, which has no read/stats API on
# the hobby tier, so the manager currently runs reviewer-only (no env vars
# needed here). See agents/shared/analytics.py to wire a queryable source later.
